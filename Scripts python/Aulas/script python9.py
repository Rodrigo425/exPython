import random
num = random.randint(1, 10)
print(num)

#A biblioteca random pede para o computador gerar um número aleatorio float entre 0 e 1
#E o randint() serve para gerar um número aleatorio inteiro dentro do randint(valor, valor2) entre dois valores exemplo valor1, valor2 